name: livnium_core
description: A starting point for Dart libraries or applications.
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'   # <-- bump to at least 3.3 for extension types

dependencies:
  args: ^2.7.0
  # js: ^0.7.2           # <-- remove; we're using dart:js_interop

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3

executables:
  livnium: livnium
