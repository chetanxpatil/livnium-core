# Alphabet API

```dart
const int kRadix = 27;
int? symbolToValue(String ch);
String? valueToSymbol(int v);
List<int>? stringToDigits(String s);
String digitsToString(List<int> d);
bool isValidWord(String s);
```
